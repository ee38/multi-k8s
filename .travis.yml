sudo: required
services:
  - docker

before_install:
  - docker build -t ee38/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true ee38/react-test npm test

after_success:
  - docker build -t ee38/multi-client ./client
  - docker build -t ee38/multi-nginx ./nginx
  - docker build -t ee38/multi-server ./server
  - docker build -t ee38/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to the docker hub
  - docker push ee38/multi-client
  - docker push ee38/multi-nginx
  - docker push ee38/multi-server
  - docker push ee38/multi-worker